'use strict';

/**
 * customer-order-cuisine service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::customer-order-cuisine.customer-order-cuisine');
